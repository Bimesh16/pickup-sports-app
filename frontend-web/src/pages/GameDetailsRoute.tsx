import React from 'react';
import GameDetails from './dashboard/GameDetails';

// Wrapper component to match the route structure expected by App.tsx
export default function GameDetailsRoute() {
  return <GameDetails />;
}
