# Frontend environment (Vite)

# Base URL for your backend API (used when USE_MOCK=false in src/lib/http.ts)
# Example: https://api.your-backend.example.com
VITE_API_BASE=https://api.your-backend.example.com

# Optional: environment flag surfaced in the UI (used by EnvBadge when hitting /config/flags)
# VITE_ENV=local

